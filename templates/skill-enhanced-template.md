---
name: {skill-name}
description: |
  {技术名} {版本} {领域}专业知识。
  ① 帮我干什么：{核心能力1}、{核心能力2}、{核心能力3}
  ② 什么时候出场：当涉及{触发场景}时自动调用
  ③ 和项目有无关系：{项目相关性说明}

  【语义触发】AI 识别{领域}相关任务时激活。
  关键词：{keyword1}、{keyword2}、{keyword3}。
version: 1.0.0
allowed-tools: Read, Grep, Glob, Task
---

# {技术名} {版本} 开发指南

> **技术版本**: {技术名} {版本} | {依赖框架}
> **核心理念**: {一句话核心理念}

---

## Quick Reference（快速查阅）

> 借鉴 makepad-skills 模式：提供快速可查阅的 API 参考表

### {核心概念1} 属性速查表

| 属性 | 类型 | 用途 | 示例 |
|------|------|------|------|
| `property1` | `string` | 描述 | `"value"` |
| `property2` | `boolean` | 描述 | `true` |

### {核心概念2} 属性速查表

| 属性 | 类型 | 用途 | 示例 |
|------|------|------|------|
| `property1` | `string` | 描述 | `"value"` |

### 常用 API/Hooks

| API/Hook | 用途 | 返回值 |
|----------|------|--------|
| `useXxx()` | 获取 xxx | `Type` |
| `useYyy()` | 处理 yyy | `Type` |

---

## 项目架构

```
src/features/{feature}/
├── components/           # 组件
├── hooks/                # 自定义 Hooks
├── state/                # 状态管理
└── utils/                # 工具函数
```

---

## 1. 硬性约束 (Hard Constraints)

| 约束 | 规则 | 审计命令 | 严重度 |
|------|------|----------|--------|
| {约束1} | {规则描述} | `grep -rn "pattern" src/` | 🔴 Critical |
| {约束2} | {规则描述} | `grep -rn "pattern" src/` | 🟡 Warning |

---

## 2. 反模式 (Anti-Patterns)

### 反模式 2.1: {问题名称}

**问题**：{问题描述}

**检测**：
```bash
# 检测命令
grep -rn "pattern" src/ --include="*.ts"
```

**修正**：
```typescript
// ❌ 错误
const wrong = badPattern();

// ✅ 正确
const correct = goodPattern();
```

---

## 3. 最佳实践 (Golden Paths)

### 3.1 {实践名称}

```typescript
// 完整的最佳实践代码示例
export function bestPractice() {
  // 实现
}
```

---

## 4. 自我验证 (Self-Verification)

### 审计脚本

```bash
#!/bin/bash
# {skill-name}-audit.sh

echo "🔄 {技术名} 合规审计"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

ERRORS=0

# 检查项 1
echo -e "\n📍 检测 {检查项1}..."
# 检测逻辑

echo -e "\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
if [ $ERRORS -eq 0 ]; then
    echo "✅ 审计通过"
    exit 0
else
    echo "❌ 发现 $ERRORS 个问题"
    exit 1
fi
```

### 快速检查清单

- [ ] {检查项1}
- [ ] {检查项2}
- [ ] {检查项3}

---

## 5. Common Mistakes（常见错误速查）

> 借鉴 makepad-skills 的 ✅/❌ 对照格式

### 5.1 {错误类别1}

```typescript
// ❌ WRONG - {为什么错}
const wrong = badCode();

// ✅ CORRECT - {为什么对}
const correct = goodCode();
```

### 错误速查表

| 错误 | 原因 | 修复 |
|------|------|------|
| {错误现象1} | {原因} | {修复方法} |
| {错误现象2} | {原因} | {修复方法} |

---

## 6. Complete Examples（完整示例）

### 6.1 {场景1} 完整模板

```typescript
// 完整可复制粘贴的代码示例
import { xxx } from 'library';

export function CompleteExample() {
  // 完整实现
}
```

### 6.2 {场景2} 完整模板

```typescript
// 另一个完整示例
```

---

## 🔗 与全局 Skills 协作

| Skill | 协作方式 |
|-------|----------|
| `{skill1}` | {协作说明} |
| `{skill2}` | {协作说明} |

### 关联文件

- `src/features/{feature}/**/*.ts`

---

## References（参考资源）

- [{技术名} 官方文档]({url})
- [{技术名} 示例]({url})

---

**✅ {Skill Name} v{version}** | **增强型模板已集成**
