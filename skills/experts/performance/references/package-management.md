# 包管理决策指南

> **核心原则**: 每引入一个包都是技术债务。必须证明 ROI 才能引入。
> **来源**: [Bundlephobia](https://bundlephobia.com) + 项目实战经验

---

## 引入新包前的 5 问决策框架

### 问题 1: 真的需要引入吗？

```
┌─ 必要性评估 ─────────────────────────────────────┐
│                                                  │
│  □ 原生 API 能实现吗？                            │
│    ├── Fetch API vs Axios                        │
│    ├── IntersectionObserver vs 滚动库             │
│    ├── CSS Grid/Flexbox vs 布局库                 │
│    └── Web Animations API vs 动画库               │
│                                                  │
│  □ 项目已有类似功能吗？                           │
│    ├── 检查 src/utils/                           │
│    ├── 检查 src/hooks/                           │
│    └── 检查 package.json 已有依赖                 │
│                                                  │
│  □ 10 行代码能解决吗？                            │
│    └── 简单功能自己写，复杂功能用成熟库            │
│                                                  │
└──────────────────────────────────────────────────┘
```

**示例对比**:

| 需求 | ❌ 引入包 | ✅ 原生实现 |
|------|---------|-----------|
| 防抖 | lodash.debounce (~1KB) | 10 行自写代码 |
| 深拷贝 | lodash.cloneDeep (~18KB) | structuredClone() (原生) |
| 日期格式化 | moment.js (~70KB) | Intl.DateTimeFormat (原生) |
| 懒加载图片 | react-lazy-load | loading="lazy" (原生) |
| 本地存储 | localforage (~29KB) | localStorage + try/catch |

---

### 问题 2: 包质量如何？

```
┌─ 质量评估清单 ───────────────────────────────────┐
│                                                  │
│  □ npm 周下载量 > 10,000?                        │
│    └── 下载量低 = 社区验证不足，风险高            │
│                                                  │
│  □ 最近 6 个月有更新？                           │
│    └── 无更新 = 可能已废弃或无人维护              │
│                                                  │
│  □ GitHub Stars > 1,000?                         │
│    └── 低 Stars = 知名度低，问题难找解决方案       │
│                                                  │
│  □ 无已知安全漏洞？                              │
│    ├── npm audit 检查                            │
│    └── Snyk 漏洞数据库                           │
│                                                  │
│  □ TypeScript 支持？                             │
│    ├── 原生 TS 最佳                              │
│    ├── @types/* 次选                             │
│    └── 无类型 = 维护成本高                        │
│                                                  │
│  □ 许可证兼容？                                  │
│    ├── MIT / Apache 2.0 = 安全                   │
│    ├── GPL = 谨慎（有传染性）                     │
│    └── 无许可证 = 风险高                          │
│                                                  │
└──────────────────────────────────────────────────┘
```

**快速检查命令**:

```bash
# 检查包信息
npm info <package-name>

# 检查安全漏洞
npm audit

# 检查过时依赖
npm outdated
```

---

### 问题 3: 包大小影响多少？

```
┌─ 大小评估标准 ───────────────────────────────────┐
│                                                  │
│  首屏包影响阈值:                                  │
│  ├── < 5KB gzip   = 🟢 可接受                    │
│  ├── 5-20KB gzip  = 🟡 需评估                    │
│  └── > 20KB gzip  = 🔴 必须论证                  │
│                                                  │
│  检查工具:                                        │
│  ├── bundlephobia.com (在线)                     │
│  ├── npm install --dry-run (本地)                │
│  └── webpack-bundle-analyzer (构建后)            │
│                                                  │
└──────────────────────────────────────────────────┘
```

**常见包大小参考**:

| 包名 | Gzip 大小 | 评估 |
|------|----------|------|
| lodash (全量) | ~70KB | 🔴 禁止全量 |
| lodash-es/debounce | ~1KB | 🟢 按需引入 |
| moment.js | ~70KB | 🔴 使用 dayjs |
| dayjs | ~2KB | 🟢 推荐 |
| axios | ~14KB | 🟡 考虑 ky |
| ky | ~3KB | 🟢 轻量替代 |
| date-fns | ~70KB (全量) | 🟡 按需引入 |
| date-fns/format | ~2KB | 🟢 按需引入 |

---

### 问题 4: 框架兼容吗？

```
┌─ 兼容性检查 ─────────────────────────────────────┐
│                                                  │
│  当前项目技术栈 (从 package.json 读取):           │
│  ├── React 版本: 检查 peer dependencies          │
│  ├── TypeScript 版本: 检查类型支持               │
│  └── 构建工具: Vite/Webpack 兼容性               │
│                                                  │
│  检查项:                                          │
│  □ 支持当前 React 版本？                          │
│  □ 支持 TypeScript strict mode？                 │
│  □ 支持 ESM (ES Modules)？                       │
│  □ 支持 Tree Shaking？                           │
│  □ SSR 兼容（如需要）？                           │
│                                                  │
└──────────────────────────────────────────────────┘
```

**兼容性示例**:

```typescript
// ❌ 不兼容 ESM 的包（CJS only）
const pkg = require('old-package');

// ✅ ESM 兼容
import { func } from 'modern-package';

// ❌ 不支持 Tree Shaking
import _ from 'lodash';

// ✅ 支持 Tree Shaking
import debounce from 'lodash-es/debounce';
```

---

### 问题 5: 怎么引入？

**引入方式优先级**:

```
1. 按需引入 (Best)     → import { func } from 'pkg/func'
2. 动态导入 (Good)     → const { func } = await import('pkg')
3. 全量引入 (Avoid)    → import pkg from 'pkg'
```

---

## 引入方式详解

### 1. 按需引入（首选）

```typescript
// ✅ 正确：按需引入
import debounce from 'lodash-es/debounce';
import { format } from 'date-fns';
import { Button, Modal } from 'antd';

// ❌ 错误：全量引入
import _ from 'lodash';
import * as dateFns from 'date-fns';
import antd from 'antd';
```

**Babel/SWC 配置（自动按需）**:

```javascript
// babel.config.js (antd 示例)
module.exports = {
  plugins: [
    ['import', { libraryName: 'antd', style: true }]
  ]
};
```

---

### 2. 动态导入（非首屏）

```typescript
// ✅ 路由级代码分割
const EditorPage = React.lazy(() => import('./pages/Editor'));

// ✅ 组件级动态导入（大型库）
const loadMermaid = async () => {
  const { default: mermaid } = await import('mermaid');
  return mermaid;
};

// ✅ 条件动态导入
if (needsExcelExport) {
  const { write } = await import('xlsx');
  write(data, 'export.xlsx');
}
```

**动态导入最佳实践**:

| 场景 | 策略 |
|------|------|
| 路由页面 | React.lazy + Suspense |
| 大型图表库 | 用户触发时动态加载 |
| 编辑器组件 | 进入编辑模式时加载 |
| 导出功能 | 点击导出按钮时加载 |
| polyfill | 检测不支持时加载 |

---

### 3. 全量引入（最后手段）

**仅在以下情况使用**:

- 包不支持 Tree Shaking
- 需要使用 80%+ 的功能
- 包本身很小（< 5KB gzip）

```typescript
// 可接受：整个包都需要
import dayjs from 'dayjs';

// 可接受：库本身支持按需
import { ConfigProvider } from 'antd';
```

---

## 项目特定配置

### 基于 package.json 的检查

```typescript
// 检查脚本示例
const checkNewPackage = async (packageName: string) => {
  // 1. 检查项目已有依赖
  const pkg = await import('./package.json');
  const existingDeps = {
    ...pkg.dependencies,
    ...pkg.devDependencies
  };

  // 2. 检查是否有类似功能
  const similarPackages = {
    'axios': ['ky', 'fetch'], // 项目使用 ky
    'moment': ['dayjs'],      // 项目使用 dayjs
    'lodash': ['lodash-es'],  // 必须用 ES 版本
  };

  if (similarPackages[packageName]) {
    console.warn(`考虑使用: ${similarPackages[packageName]}`);
  }

  // 3. 检查大小
  // 使用 bundlephobia API
};
```

---

## 常见替代方案

### HTTP 请求

| 原包 | 替代 | 节省 |
|------|------|------|
| axios (~14KB) | ky (~3KB) | -11KB |
| axios (~14KB) | fetch (原生) | -14KB |
| superagent (~18KB) | ky (~3KB) | -15KB |

### 日期处理

| 原包 | 替代 | 节省 |
|------|------|------|
| moment.js (~70KB) | dayjs (~2KB) | -68KB |
| moment.js (~70KB) | date-fns (按需) | -65KB |
| moment.js (~70KB) | Intl API (原生) | -70KB |

### 工具函数

| 原包 | 替代 | 节省 |
|------|------|------|
| lodash (全量 ~70KB) | lodash-es (按需) | -60KB |
| underscore (~16KB) | lodash-es (按需) | -15KB |
| ramda (~50KB) | 原生 + lodash-es | -45KB |

### 状态管理

| 原包 | 替代 | 节省 |
|------|------|------|
| redux + toolkit (~40KB) | zustand (~3KB) | -37KB |
| mobx (~60KB) | zustand (~3KB) | -57KB |

---

## 决策流程图

```
新包需求
    │
    ▼
┌─ 原生 API 能实现吗？ ─┐
│                      │
├── 是 ───────────────▶ 使用原生 API，结束
│
├── 否
│   │
│   ▼
│ ┌─ 项目已有类似依赖？ ─┐
│ │                     │
│ ├── 是 ──────────────▶ 复用现有依赖，结束
│ │
│ ├── 否
│ │   │
│ │   ▼
│ │ ┌─ 10 行代码能解决？ ─┐
│ │ │                    │
│ │ ├── 是 ─────────────▶ 自己实现，结束
│ │ │
│ │ ├── 否
│ │ │   │
│ │ │   ▼
│ │ │ ┌─ 包质量评估通过？ ─┐
│ │ │ │                   │
│ │ │ ├── 否 ────────────▶ 寻找替代包
│ │ │ │
│ │ │ ├── 是
│ │ │ │   │
│ │ │ │   ▼
│ │ │ │ ┌─ 大小 < 20KB gzip? ─┐
│ │ │ │ │                     │
│ │ │ │ ├── 否 ──────────────▶ 必须论证或找轻量替代
│ │ │ │ │
│ │ │ │ ├── 是
│ │ │ │ │   │
│ │ │ │ │   ▼
│ │ │ │ │ ┌─ 框架兼容？ ─┐
│ │ │ │ │ │              │
│ │ │ │ │ ├── 否 ───────▶ 不引入
│ │ │ │ │ │
│ │ │ │ │ ├── 是 ───────▶ ✅ 按需引入
│ │ │ │ │ │
└─┴─┴─┴─┴─┴──────────────┘
```

---

## 禁止行为

| 行为 | 状态 | 正确做法 |
|------|------|---------|
| 全量引入 lodash | ❌ **Forbidden** | 使用 lodash-es 按需引入 |
| 引入 moment.js | ❌ **Forbidden** | 使用 dayjs 或 date-fns |
| 未检查大小直接安装 | ❌ **Forbidden** | 先在 bundlephobia 检查 |
| 引入不兼容 ESM 的包 | ❌ **Forbidden** | 寻找 ESM 替代 |
| 重复功能的包 | ❌ **Forbidden** | 复用现有依赖 |

---

## 推荐工具

| 工具 | 用途 | 链接 |
|------|------|------|
| Bundlephobia | 检查包大小 | bundlephobia.com |
| npm trends | 包流行度对比 | npmtrends.com |
| Snyk | 安全漏洞检查 | snyk.io |
| webpack-bundle-analyzer | 构建分析 | npm package |
| source-map-explorer | 包大小可视化 | npm package |

---

**记住**: 最好的依赖是不需要的依赖。每个包都是维护负担。

