# Pre-Development Check（开发前检查）

> **触发时机**：每个开发任务开始前自动执行
> **目标**：复用已有资源 + 避免历史踩坑

---

## 🎯 触发条件

| 权重 | 动作 |
|------|------|
| ≥ 5 | 自动执行完整检查 |
| 2-4 | 仅检查踩坑提醒 |
| 0-1 | 跳过（纯分析/文档任务） |

---

## 📋 检查流程

```
┌─────────────────────────────────────────────────────────┐
│                   开发前检查流程                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Step 1: 🔍 复用检查                                     │
│  ├── 扫描 src/components/, src/services/, src/hooks/    │
│  ├── 语义匹配用户需求                                    │
│  └── 输出可复用资源清单                                  │
│                                                         │
│  Step 2: ⚠️ 踩坑提醒                                     │
│  ├── 读取全局踩坑: ~/.ai-knowledge/global/pitfalls.md   │
│  ├── 读取领域踩坑: ~/.ai-knowledge/domains/{domain}/    │
│  ├── 读取项目踩坑: ~/.ai-knowledge/projects/{project}/  │
│  └── 匹配当前任务相关的踩坑记录                          │
│                                                         │
│  Step 3: 📊 输出检查报告                                 │
│  └── 综合复用建议 + 踩坑提醒                             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🔍 Step 1: 复用检查

详见 `skills/patterns/component-reuse/SKILL.md`

**快速执行**:
```bash
# 扫描组件
find src/components -name "*.tsx" | head -20

# 扫描服务
find src/services -name "*.ts" | head -20

# 扫描 Hooks
find src/hooks -name "use*.ts" | head -20
```

---

## ⚠️ Step 2: 踩坑提醒

### 知识库路径

| 层级 | 路径 | 说明 |
|------|------|------|
| 全局 | `~/.ai-knowledge/global/pitfalls.md` | 跨项目通用 |
| 领域 | `~/.ai-knowledge/domains/{domain}/pitfalls.md` | 技术栈特定 |
| 项目 | `~/.ai-knowledge/projects/{project}/pitfalls.md` | 项目特定 |

### 领域自动检测

根据任务关键词自动匹配领域：

| 关键词 | 领域 |
|--------|------|
| React, 组件, 样式, 布局 | `frontend` |
| API, 数据库, 后端 | `backend` |
| 部署, CI/CD, Docker | `devops` |
| 模型, AI, 推理 | `ai-ml` |

### 匹配逻辑

```python
# 伪代码
def match_pitfalls(task_keywords, pitfalls):
    matches = []
    for pitfall in pitfalls:
        # 标题匹配
        if any(kw in pitfall.title for kw in task_keywords):
            matches.append(pitfall)
        # 标签匹配
        if any(kw in pitfall.tags for kw in task_keywords):
            matches.append(pitfall)
    return matches
```

---

## 📊 Step 3: 输出检查报告

### 报告模板

```markdown
## 🔍 开发前检查报告

### 任务: [任务描述]
### 检测领域: [frontend/backend/...]

---

### 📦 可复用资源

| 资源 | 位置 | 匹配度 | 建议 |
|------|------|--------|------|
| UserAvatar | src/components/common/UserAvatar.tsx | 95% | 直接使用 |
| useDebounce | src/hooks/useDebounce.ts | 80% | 可扩展 |

---

### ⚠️ 相关踩坑提醒

#### 1. [2026-01-05] useEffect 依赖数组缺失导致 stale closure
**现象**: 回调函数引用了过期的状态值
**预防**: 确保 useEffect 依赖数组包含所有引用的变量

#### 2. [2026-01-05] Zustand selector 返回新对象导致无限重渲染
**现象**: 每次渲染都触发 store 更新
**预防**: 使用 shallow 比较或返回原始值

---

### ✅ 检查完成，可以开始开发
```

---

## 🔧 配置

### triggers.yaml 集成

```yaml
skill_triggers:
  pre_dev_check:
    condition: "task_start AND weight >= 2"
    sequence:
      - "component_reuse_check"
      - "pitfall_reminder"
    skill: "skills/workflow/pre-dev-check/SKILL.md"
```

### 手动调用

```
/pre-check 创建用户管理模块
```

---

## 🔗 关联资源

| 资源 | 路径 |
|------|------|
| 复用检查 Skill | `skills/patterns/component-reuse/SKILL.md` |
| 触发器配置 | `configs/triggers.yaml` |
| 全局踩坑 | `~/.ai-knowledge/global/pitfalls.md` |
| 领域踩坑 | `~/.ai-knowledge/domains/*/pitfalls.md` |
| 项目踩坑 | `~/.ai-knowledge/projects/*/pitfalls.md` |

---

**Last Updated**: 2026-01-08
