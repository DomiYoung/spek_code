# JavaScript 机制扩展

## this 绑定规则
- new > 显式绑定 > 隐式绑定 > 默认绑定。
- 箭头函数继承外层 this。

## 作用域与闭包
- 词法作用域决定变量查找。
- 闭包持有引用会延长生命周期。

## 原型链
- 属性查找沿 prototype 链向上。
- 跨 realm 会影响 instanceof。

## 事件循环
- 微任务优先于宏任务；微任务过多会阻塞渲染。

## 类型转换
- 隐式转换可能导致不一致（== vs ===）。
