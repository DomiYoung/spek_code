---
name: component-reuse
description: |
  组件复用专家。开发新功能前自动检查可复用资源，避免重复造轮子。
  关键词：复用、组件、服务、Hook、创建组件、新增、实现。
allowed-tools: Read, Grep, Glob
---

# Component Reuse Expert

> **触发时机**：开发新功能前自动检查可复用资源
> **目标**：避免重复造轮子，提高代码复用率

---

## 🎯 触发条件

| 触发场景 | 关键词 |
|---------|--------|
| 新建组件 | "创建组件"、"新增"、"实现" |
| 新建服务 | "创建服务"、"API 封装" |
| 新建 Hook | "创建 hook"、"自定义 hook" |
| 功能开发 | 权重 ≥ 5 的新功能任务 |

---

## 📋 检查流程

### Step 1: 扫描现有资源

```bash
# 组件目录
find src/components -name "*.tsx" -type f

# 服务目录
find src/services -name "*.ts" -type f

# Hooks 目录
find src/hooks -name "*.ts" -type f

# 工具函数
find src/utils -name "*.ts" -type f
```

### Step 2: 语义匹配

根据用户需求，在以下维度进行匹配：

| 维度 | 检查内容 |
|------|---------|
| **名称相似** | 组件/函数名与需求关键词匹配 |
| **功能相似** | 通过注释/JSDoc 判断功能重叠 |
| **模式相似** | 相同的 UI 模式（表格、表单、弹窗等） |
| **依赖相似** | 使用相同的第三方库 |

### Step 3: 输出复用建议

```markdown
## 🔍 复用检查报告

### 可直接复用 ✅
| 资源 | 位置 | 匹配度 | 说明 |
|------|------|--------|------|
| xxx | src/components/xxx | 90% | 直接使用 |

### 可扩展复用 ⚠️
| 资源 | 位置 | 需要修改 |
|------|------|---------|
| yyy | src/hooks/yyy | 添加参数支持 |

### 无可复用资源 ❌
需要新建，建议参考：
- 项目模式: ~/.ai-knowledge/projects/{project}/patterns.md
- 领域模式: ~/.ai-knowledge/domains/{domain}/patterns.md
```

---

## 🔧 检查目录配置

### 默认检查目录

```yaml
components:
  - src/components/
  - src/features/*/components/

services:
  - src/services/
  - src/api/

hooks:
  - src/hooks/
  - src/features/*/hooks/

utils:
  - src/utils/
  - src/lib/
```

### 项目自定义

在项目 `CLAUDE.md` 中可覆盖：

```markdown
## 复用检查目录
- 组件: src/components/, src/shared/
- 服务: src/services/
- Hooks: src/hooks/
```

---

## 🚀 自动触发

### 通过 triggers.yaml 触发

```yaml
skill_triggers:
  component_reuse:
    condition: "before_new_component"
    directories:
      - "src/components/"
      - "src/services/"
      - "src/hooks/"
    skill: "skills/patterns/component-reuse/SKILL.md"
```

### 手动调用

```
/reuse-check 创建用户头像组件
```

---

## 📊 复用率统计

任务完成后，记录复用情况：

| 指标 | 说明 |
|------|------|
| 复用率 | 复用资源数 / 总资源数 |
| 节省代码行 | 估算避免重复编写的代码量 |
| 复用来源 | 来自哪些模块 |

---

## 💡 最佳实践

1. **先搜后建**：开发前先搜索，不要假设没有可复用的
2. **抽象沉淀**：复用 2 次以上的代码，考虑抽象为通用组件
3. **文档标注**：可复用组件要有清晰的 JSDoc/注释
4. **模式记录**：新的通用模式记录到 patterns.md

---

## 🔗 关联 Skill

- `skills/experts/frontend/SKILL.md` - 前端专家
- `skills/workflow-orchestrator/SKILL.md` - 工作流编排

---

**Last Updated**: 2026-01-08
