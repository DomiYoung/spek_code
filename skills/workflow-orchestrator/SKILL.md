---
name: workflow-orchestrator
description: "工作流编排专家。当用户提出复杂需求时自动激活：(1) 分析需求，决定调用哪些 skills (2) 编排 skills 调用顺序 (3) 验证每个 skill 输出结果 (4) 确保端到端交付质量。作为 skills 系统的元协调器。"
---

# Workflow Orchestrator Expert

> **核心理念**：分解 > 编排 > 验证 > 交付

## 触发条件

当用户提出以下类型需求时**自动激活**：
- 涉及多个领域的复杂需求
- "我想做一个完整的 XX 功能"
- 需要前后端协同的任务
- 从需求到交付的全流程任务

## 工作流程

### Phase 1: 需求分析与 Skills 匹配

```
用户需求
    ↓
┌─────────────────────────────────────────┐
│ 1. 解析需求关键词                        │
│ 2. 匹配触发器映射表                      │
│ 3. 确定需要调用的 skills                 │
│ 4. 排序调用优先级                        │
└─────────────────────────────────────────┘
```

### Phase 2: Skills 编排矩阵

根据任务类型选择编排模式：

| 任务类型 | 编排顺序 |
|:---|:---|
| **新功能开发** | `product-manager` → `senior-architect` → `database-expert` → `backend-expert/flask-expert` → `frontend-design` → `webapp-testing` |
| **前端组件** | `frontend-design` → `react-expert` → `framer-motion-expert` → `tailwindcss-expert` |
| **后端 API** | `senior-architect` → `database-expert` → `flask-expert` → `backend-expert` |
| **系统重构** | `architecture-review` → `senior-architect` → `code-quality-gates` |
| **数据库设计** | `database-expert` → `senior-architect` |
| **性能优化** | `database-expert` (SQL) → `backend-expert` → `frontend-expert` |

### Phase 3: 执行与输出验证

对每个 skill 的输出执行验证：

```markdown
## Skill 输出验证清单

### product-manager 输出
- [ ] PRD 包含用户故事
- [ ] 验收标准使用 Given-When-Then
- [ ] 范围界定清晰 (P0/P1/不做)

### senior-architect 输出
- [ ] 包含 C4 图或架构图
- [ ] 有 ADR 决策记录
- [ ] 技术选型有权衡分析

### database-expert 输出
- [ ] Schema 使用规范命名
- [ ] 有索引策略
- [ ] 考虑了迁移方案

### flask-expert / backend-expert 输出
- [ ] API 遵循 RESTful 规范
- [ ] 有错误处理
- [ ] 有验证逻辑

### frontend-design 输出
- [ ] 视觉独特，非 AI 通用风格
- [ ] 有动效设计
- [ ] 响应式布局

### webapp-testing 输出
- [ ] 测试覆盖关键路径
- [ ] 有断言验证
```

### Phase 4: 交付确认

```markdown
## 交付检查清单

- [ ] 所有 skills 输出已验证
- [ ] 代码已提交到 Git
- [ ] 文档已更新
- [ ] 测试已通过
```

## 编排示例

### 示例：用户说 "我想做一个用户认证功能"

```
1. 需求分析
   → 涉及：后端 API、数据库、前端 UI
   
2. Skills 匹配
   → product-manager (PRD)
   → senior-architect (系统设计)
   → database-expert (users 表设计)
   → flask-expert (认证 API)
   → frontend-design (登录/注册页面)
   → webapp-testing (端到端测试)

3. 执行顺序
   [1] product-manager → 输出 PRD → 验证 ✓
   [2] senior-architect → 输出架构图 → 验证 ✓
   [3] database-expert → 输出 Schema → 验证 ✓
   [4] flask-expert → 输出 API 代码 → 验证 ✓
   [5] frontend-design → 输出 UI 代码 → 验证 ✓
   [6] webapp-testing → 输出测试 → 验证 ✓

4. 交付确认
   → 所有输出符合验收标准 ✓
```

## 验证规则

### 输出质量评分

| 维度 | 权重 | 评分标准 |
|:---|:---|:---|
| 完整性 | 30% | 包含所有必要元素 |
| 准确性 | 30% | 符合领域最佳实践 |
| 一致性 | 20% | 与其他 skills 输出协调 |
| 可执行性 | 20% | 可直接落地实施 |

### 不合格处理

如果某个 skill 输出不合格：
1. 记录具体问题
2. 反馈给用户
3. 重新调用该 skill
4. 再次验证

## 与其他 Skills 的关系

```
workflow-orchestrator (顶层编排)
├── product-manager (需求层)
├── senior-architect (设计层)
│   └── database-expert
├── flask-expert / backend-expert (后端层)
├── frontend-design / frontend-expert (前端层)
└── webapp-testing (验证层)
```

---

## 🛡️ 治理 Skills 集成（2025-12-28 升级）

### 治理层编排

除了功能开发编排，workflow-orchestrator 还需协调治理层 Skills：

```
治理编排矩阵：

会话启动
├── 读取 KI (pitfalls.md, component_patterns.md)
└── 读取 SESSION.md (project-session-management)

功能设计阶段
├── component-reuse-expert (复用检查)
├── spec-first-development (规范创建)
└── product-manager (PRD 输出)

功能开发阶段
├── Hooks 保护 (protect-specs.py 拦截规范修改)
├── [执行层 Skills]
└── workflow-orchestrator (协调)

功能完成阶段
├── product-manager (Checklist 核对)
├── ki-manager (知识沉淀)
└── 更新 SESSION.md
```

### 与治理 Skills 的协作关系

| Skill | 角色 | 触发时机 |
|:---|:---|:---|
| `project-session-management` | 状态管理 | 会话启动/恢复 |
| `component-reuse-expert` | 复用检查 | brainstorming 阶段 |
| `spec-first-development` | 规范创建 | 设计阶段 |
| `Hooks (protect-specs.py)` | 需求冻结 | Edit/Write specs/ 时 |
| `ki-manager` | 知识沉淀 | 功能完成后 |
| `product-manager` | 进度汇报 | 需要时 |

### 完整工作流示例

```
用户: "我想做一个用户认证功能"

治理层:
  [1] 读取 KI → 检查历史踩坑
  [2] component-reuse-expert → 检查可复用组件

执行层 (原有编排):
  [3] product-manager → PRD
  [4] spec-first-development → 创建 specs/auth/
  [5] senior-architect → 架构设计
  [6] database-expert → 数据模型
  [7] flask-expert → API 实现
  [8] frontend-design → UI 实现
  [9] webapp-testing → 端到端测试

治理层:
  [10] product-manager → Checklist 核对
  [11] ki-manager → 沉淀经验
  [12] 更新 SESSION.md
```

---

## 进化日志

| 日期 | 更新内容 | 来源 |
|:---|:---|:---|
| 2025-12-28 | 新增治理 Skills 集成，完善编排矩阵 | 治理体系审核 |
| 2025-12-27 | 初始版本 | 用户需求 |
